---
phase: 02-analyzer-workflow
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .claude/commands/script-generator.md
autonomous: false
requirements:
  - ANLZ-05
  - ANLZ-06

must_haves:
  truths:
    - "After user approves the draft, Claude writes the script file to niches/{niche}/scripts/ with correct YAML frontmatter and body"
    - "The script filename follows {NNN}-{hook-type-slug}.md where NNN is the next sequential 3-digit number from the scripts/ directory"
    - "After saving, Claude re-reads all scripts/ and rewrites patterns.md with updated cross-script synthesis"
    - "After saving, Claude shows a one-line summary with filename, tier, and view count, plus a progress hint toward system-prompt.md generation"
    - "When scripts/ has 3+ files AND system-prompt.md still has STATUS placeholder, Claude generates a system prompt draft and waits for approval before writing"
    - "system-prompt.md generation triggers after the 3rd script is saved, not before"
  artifacts:
    - path: ".claude/commands/script-generator.md"
      provides: "Complete Analyzer workflow including file-write path, patterns.md synthesis, system-prompt.md generation"
      contains: "Step 5 through Step 7, post-save logic, approval detection rules"
    - path: "niches/ai-commentary/scripts/001-{slug}.md"
      provides: "First training script written by the Analyzer"
      min_lines: 20
    - path: "niches/ai-commentary/patterns.md"
      provides: "Updated cross-script synthesis (no longer placeholder STATUS)"
  key_links:
    - from: ".claude/commands/script-generator.md"
      to: "niches/{niche}/scripts/{NNN}-{slug}.md"
      via: "Step 5 write instruction"
      pattern: "write.*scripts"
    - from: ".claude/commands/script-generator.md"
      to: "niches/{niche}/patterns.md"
      via: "Step 6 rewrite instruction after every save"
      pattern: "rewrite.*patterns"
    - from: ".claude/commands/script-generator.md"
      to: "niches/{niche}/system-prompt.md"
      via: "Step 6 count check after write"
      pattern: "count.*scripts|system-prompt.*STATUS"
---

<objective>
Complete the `.claude/commands/script-generator.md` skill file by adding:
- Step 5: Approval detection and file-writing logic (ANLZ-05)
- Step 6: Post-save operations — patterns.md synthesis and system-prompt.md generation trigger (ANLZ-06)
- Step 7: Save summary UX with progress hints
- Human verification checkpoint: user runs the full Analyzer workflow with a real transcript

Purpose: ANLZ-05 and ANLZ-06 require the write path and system-prompt.md generation logic to be present and correct in the skill file.
Output: Complete, end-to-end Analyzer skill. Verified by human running `/script-generator` with a sample transcript.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analyzer-workflow/02-CONTEXT.md
@.planning/phases/02-analyzer-workflow/02-RESEARCH.md
@.planning/phases/02-analyzer-workflow/02-01-SUMMARY.md

<interfaces>
<!-- Existing file state coming into Plan 02 -->

From .claude/commands/script-generator.md (created by Plan 01):
- Sections present: Role, On Invocation, Analyzer Path Steps 1-4, Generator Path placeholder, Constraints
- Step 4 ends with approval gate ("Does this look right? Reply with 'save' to write to disk, or tell me what to change.")
- No file-writing logic yet — Plan 02 adds Steps 5-7

From niches/ai-commentary/system-prompt.md:
- Contains: "STATUS: Not yet generated." — this is the detection signal
- Trigger: count >= 3 scripts in scripts/ AND this STATUS line is still present

From niches/ai-commentary/patterns.md:
- Contains: "STATUS: Not yet generated." — will be overwritten on every save

File naming convention:
- scripts/{NNN}-{hook-type-slug}.md
- NNN: 3-digit zero-padded, next number after current max in scripts/ (start at 001 if empty)
- hook-type-slug: kebab-case slug from hook_type frontmatter field

patterns.md recommended format (from RESEARCH.md):
# Patterns: {niche}
**Last updated:** {date} — {N} scripts analyzed
---
## Hook Patterns
## Pacing Patterns
## Sentence Structure Norms
## Storytelling Approaches
## What Consistently Works

Note for 1-script patterns.md: include "Patterns from 1 script — limited synthesis available. Add more scripts for reliable patterns."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add write path and post-save operations to skill file</name>
  <files>.claude/commands/script-generator.md</files>
  <action>
Append three new steps to the `## Analyzer Path` section in `.claude/commands/script-generator.md`, after the existing Step 4 (Show Draft). Do not modify Steps 1-4.

**### Step 5: Await Approval and Write Script File**

Approval detection: Any affirmative reply from the user triggers file writing. Affirmative signals include: "save", "looks good", "go ahead", "yes", "approved", or similar. Anything else is treated as a revision request — Claude revises the draft and re-displays it before asking for approval again.

When approval is received:

1. List the `niches/{selected-niche}/scripts/` directory to find the current highest NNN number. If the directory is empty, use 001. Otherwise, increment the highest existing number by 1.
2. Construct the filename: `{NNN}-{hook-type-slug}.md` where hook-type-slug is the kebab-case hook_type from the YAML (e.g., `003-contrast-reveal.md`)
3. Write the complete script file to `niches/{selected-niche}/scripts/{filename}` — include full YAML frontmatter block (---delimited) followed by `## Transcript` section (with [HOOK]/[BUILD]/[PAYOFF] labels) followed by `## Analysis` section (with 6 bold-named fields)

**### Step 6: Post-Save Operations**

After writing the script file, Claude performs TWO operations in this order:

**Operation A — Update patterns.md:**
Re-read all `.md` files in `niches/{selected-niche}/scripts/`. Synthesize patterns across all scripts and rewrite `niches/{selected-niche}/patterns.md` with this structure:

```
# Patterns: {selected-niche}

**Last updated:** {today's ISO date} — {N} scripts analyzed

---

## Hook Patterns
[Recurring hook techniques observed across scripts, with frequency notes]

## Pacing Patterns
[Energy flow and timing patterns observed across scripts]

## Sentence Structure Norms
[Length, rhythm, declarative vs. question ratios]

## Storytelling Approaches
[Setups, contrasts, reveals, and structures that recur]

## What Consistently Works
[Cross-cutting synthesis — what makes these scripts succeed]
```

If only 1 script exists, add a note at the top: "Patterns from 1 script — limited synthesis available. Add more scripts for reliable patterns."

**Operation B — Check system-prompt.md trigger:**
Count the `.md` files now in `niches/{selected-niche}/scripts/` (including the file just written).

If count < 3: skip system-prompt.md generation. This will be noted in the Step 7 summary.

If count >= 3 AND `niches/{selected-niche}/system-prompt.md` still contains the line `STATUS: Not yet generated`: generate the system prompt.

System prompt generation sequence:
1. Re-read all scripts in `scripts/` (already done for patterns.md — reuse that context)
2. Generate a niche system prompt covering:
   - Creator voice and tone (derived from writing_style patterns across scripts)
   - Niche-specific style rules (from hook_patterns, sentence_structure, pacing)
   - What makes a script succeed in this niche (from why_it_works patterns)
   - What to avoid (from anti-patterns.md if non-empty; from common weaknesses identified in analysis)
3. Display the generated system prompt as a draft: "Here's the system prompt I've derived from your 3 training scripts. Reply with 'save system prompt' to write it to disk, or tell me what to change."
4. STOP. Do not write system-prompt.md until the user explicitly approves with "save system prompt" or equivalent affirmative.

If count >= 3 but system-prompt.md does NOT contain `STATUS: Not yet generated` (already generated): skip silently.

**### Step 7: Post-Save Summary**

After completing Step 6 (and after system-prompt.md is written if triggered), Claude outputs ONE summary line:

Format when count >= 3 after this save:
```
Saved {filename} (Tier {N}, {views formatted naturally e.g. 2.4M views}). {niche} now has {count} training scripts.
```

Format when count < 3 after this save:
```
Saved {filename} (Tier {N}, {views formatted naturally}). system-prompt.md will generate after 3 scripts are ingested (currently: {count}).
```

After the summary line, Claude is ready for another transcript. It does not re-ask the opening niche/mode question — the session context is maintained.
  </action>
  <verify>
    <automated>grep -c "Step 5\|Step 6\|Step 7\|patterns.md\|system-prompt\|STATUS: Not yet generated" /c/Users/alfon/PersonalProjects/Shorts-Studio/.claude/commands/script-generator.md</automated>
  </verify>
  <done>Skill file contains Steps 5, 6, and 7. patterns.md synthesis instructions are present with the recommended section structure. system-prompt.md trigger logic is present: count AFTER write, check STATUS header, draft-then-approve UX for system prompt. Save summary formats for both count < 3 and count >= 3 cases are present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human walkthrough — verify full Analyzer workflow end-to-end</name>
  <action>Run `/script-generator` in a new Claude Code session. Follow the numbered steps in how-to-verify to exercise all 7 ANLZ requirements. Ingest 3 transcripts total to trigger system-prompt.md generation.</action>
  <verify>
    <automated>ls /c/Users/alfon/PersonalProjects/Shorts-Studio/niches/ai-commentary/scripts/ && head -5 /c/Users/alfon/PersonalProjects/Shorts-Studio/niches/ai-commentary/patterns.md && head -5 /c/Users/alfon/PersonalProjects/Shorts-Studio/niches/ai-commentary/system-prompt.md</automated>
  </verify>
  <done>User has typed "approved". All 7 ANLZ behaviors confirmed. 3 script files in scripts/, patterns.md updated, system-prompt.md generated with real content.</done>
  <what-built>
    Complete `/script-generator` Analyzer workflow. The skill file at `.claude/commands/script-generator.md` covers:
    - Opening prompt: niche + mode selection in one message
    - Guided fill input collection (one message from user)
    - Transcript analysis: [HOOK]/[BUILD]/[PAYOFF] segmentation + 6-field analysis + quality tier
    - Anti-pattern flagging inline in draft
    - Draft display with approval gate (Claude stops and waits)
    - File writing to scripts/ with sequential numbering
    - patterns.md synthesis after every save
    - system-prompt.md generation after 3rd script (draft-then-approve)
    - Save summary with progress hints
  </what-built>
  <how-to-verify>
    Run the full Analyzer workflow in a new Claude Code session. Use a real or plausible AI-commentary transcript (3-5 sentences minimum).

    **Verify ANLZ-01 and ANLZ-02:**
    1. Run `/script-generator`
    2. Confirm: opening prompt asks for niche and mode in one question
    3. Reply: `ai-commentary | Analyzer`
    4. Confirm: Claude shows guided fill prompt and waits (does NOT start analyzing yet)
    5. Paste views + optional notes + transcript in one message

    **Verify ANLZ-03 and ANLZ-04:**
    6. Confirm: draft shows three labelled segments — [HOOK], [BUILD], [PAYOFF]
    7. Confirm: draft shows exactly 6 analysis fields with bold names
    8. Confirm: quality_tier in the YAML draft is 1, 2, or 3 (matching view count thresholds)
    9. Confirm: views in YAML is a plain integer (no quotes, no abbreviations like "2.4M")

    **Verify approval gate:**
    10. Confirm: Claude shows "Does this look right?" and STOPS — no files written yet
    11. Run: `ls niches/ai-commentary/scripts/` — should be empty at this point

    **Verify ANLZ-05:**
    12. Reply: `save`
    13. Run: `ls niches/ai-commentary/scripts/` — confirm one file exists named `001-{slug}.md`
    14. Run: `head -10 niches/ai-commentary/scripts/001-*.md` — confirm YAML frontmatter is correct

    **Verify patterns.md update:**
    15. Run: `cat niches/ai-commentary/patterns.md` — confirm STATUS placeholder is gone, real content present

    **Verify ANLZ-07 (anti-pattern flagging):**
    16. Add a test phrase to `niches/ai-commentary/anti-patterns.md` (e.g., `- "you won't believe this"`)
    17. Run a second Analyzer session with a transcript containing that phrase
    18. Confirm: anti-pattern flag appears in the draft before the transcript section

    **Verify ANLZ-06 (system-prompt.md after 3rd script):**
    19. Ingest 2 more transcripts (total 3 scripts saved)
    20. After saving the 3rd script, confirm: Claude shows a system prompt draft and asks for approval
    21. Reply: `save system prompt`
    22. Run: `cat niches/ai-commentary/system-prompt.md` — confirm STATUS header is gone, real content present

    Type "approved" when all checks pass, or describe specific issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
After Plan 02 completes (post-checkpoint):
- `niches/ai-commentary/scripts/` contains at least 3 `.md` files with valid YAML frontmatter
- `niches/ai-commentary/patterns.md` no longer contains `STATUS: Not yet generated`
- `niches/ai-commentary/system-prompt.md` no longer contains `STATUS: Not yet generated`
- All 7 ANLZ requirements verified through manual walkthrough
</verification>

<success_criteria>
- Complete Analyzer workflow runs end-to-end without error: invoke to niche/mode to input to draft to approve to files written
- Script files written with correct YAML types (integer views, integer quality_tier)
- patterns.md updated after each save with structured synthesis
- system-prompt.md generated (draft-then-approve) after 3rd script is saved
- Anti-pattern flags appear inline in draft when anti-patterns.md has entries
- User has confirmed all 7 ANLZ requirements via walkthrough
</success_criteria>

<output>
After completion, create `.planning/phases/02-analyzer-workflow/02-02-SUMMARY.md` following the summary template.
</output>
