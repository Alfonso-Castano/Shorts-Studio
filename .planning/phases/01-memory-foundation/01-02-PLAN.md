---
phase: 01-memory-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - niches/ai-commentary/anti-patterns.md
  - niches/ai-commentary/feedback.md
  - niches/ai-commentary/patterns.md
  - niches/ai-commentary/system-prompt.md
autonomous: false
requirements:
  - MEM-04
  - MEM-05

must_haves:
  truths:
    - "anti-patterns.md exists with a self-explanatory schema header and an empty list ready for user content"
    - "feedback.md exists with a schema header that documents the 5-bullet cap rule and an empty list ready for user content"
    - "patterns.md exists with a header that explains it is Analyzer-generated and shows STATUS: Not yet generated"
    - "system-prompt.md exists with a header that explains it is Analyzer-generated and shows STATUS: Not yet generated"
    - "All four files can be read by Phase 2 (Analyzer) and Phase 3 (Generator) without errors from missing or malformed content"
  artifacts:
    - path: "niches/ai-commentary/anti-patterns.md"
      provides: "User-maintained list of phrases/patterns to never use in generated scripts"
      contains: "Anti-Patterns"
    - path: "niches/ai-commentary/feedback.md"
      provides: "Per-generation notes that improve future scripts, capped at 5 bullets"
      contains: "5 bullets"
    - path: "niches/ai-commentary/patterns.md"
      provides: "Cross-script synthesis layer (Analyzer writes this after training runs)"
      contains: "Not yet generated"
    - path: "niches/ai-commentary/system-prompt.md"
      provides: "Niche-specific system prompt the Generator injects as context"
      contains: "Not yet generated"
  key_links:
    - from: "niches/ai-commentary/anti-patterns.md"
      to: "Phase 2 Analyzer"
      via: "Analyzer reads before saving a script to flag matching phrases"
      pattern: "anti-patterns"
    - from: "niches/ai-commentary/feedback.md"
      to: "Phase 3 Generator"
      via: "Generator reads feedback before generating; appends new bullet when user says 'remember for next time'"
      pattern: "feedback"
---

<objective>
Create the four placeholder files that complete the `niches/ai-commentary/` memory structure. Two files are user-maintained (`anti-patterns.md`, `feedback.md`) and two are Analyzer-generated placeholders (`patterns.md`, `system-prompt.md`). Each file must be a self-documenting schema contract — clear enough that the user or a future Claude instance knows exactly what belongs in it without consulting any other document.

Purpose: Complete the memory structure so Phase 2 (Analyzer) can start writing to a fully-formed niche directory.
Output: Four placeholder files in `niches/ai-commentary/` with documented headers and empty content areas.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-memory-foundation/01-CONTEXT.md
@.planning/phases/01-memory-foundation/01-RESEARCH.md
@.planning/phases/01-memory-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create anti-patterns.md and feedback.md (user-maintained files)</name>
  <files>niches/ai-commentary/anti-patterns.md, niches/ai-commentary/feedback.md</files>
  <action>
    Create two user-maintained placeholder files in `niches/ai-commentary/`. Both need headers that function as onboarding documents — purpose, who maintains it, who reads it, format rules, and at least one commented example.

    **anti-patterns.md** — Create with this exact content:

    ```markdown
    # Anti-Patterns: ai-commentary

    **Purpose:** Phrases and patterns Claude must NEVER use when generating scripts for this niche.
    **Maintained by:** User — add entries after bad generations or when you notice clichés
    **Read by:** Analyzer (flags matches before saving a training script) and Generator (enforced on every generation call)

    **Format rules:**
    - One entry per bullet
    - Be specific: `"you won't believe this"` not `"avoid clichés"`
    - Include a brief reason if it helps: `"sounds generic — every AI channel says this"`
    - No limit on entries

    ---

    ## Anti-Patterns

    <!-- Add entries below. Examples:
    - "you won't believe this" — overused AI commentary opener
    - "let me break this down" — sounds like every other channel
    - "the implications are huge" — vague and non-committal
    -->
    ```

    **feedback.md** — Create with this exact content:

    ```markdown
    # Feedback: ai-commentary

    **Purpose:** Per-generation notes that shape how Claude writes scripts for this niche.
    **Maintained by:** User — say "remember for next time that..." during a generation session and the Generator appends it here
    **Read by:** Generator — included in every generation call as style guidance
    **Limit:** 5 bullets maximum. When a 6th bullet is added, the oldest bullet is removed.

    **Format rules:**
    - One bullet per note
    - Write it as a direct instruction: "Use shorter sentences in the hook" not "I liked when the hook was short"
    - Be specific to what changed your mind or what felt off

    ---

    ## Current Feedback

    <!-- Up to 5 bullets. Examples:
    - Use shorter sentences in the hook — punchy, not explanatory
    - The payoff should feel like a reveal, not a summary
    - Avoid starting with a question — statements hit harder for this niche
    -->
    ```
  </action>
  <verify>
    <automated>cat niches/ai-commentary/anti-patterns.md && echo "---" && cat niches/ai-commentary/feedback.md</automated>
  </verify>
  <done>`anti-patterns.md` exists with purpose/maintained-by/read-by header and commented example. `feedback.md` exists with purpose/maintained-by/read-by header, 5-bullet cap rule documented, and commented example.</done>
</task>

<task type="auto">
  <name>Task 2: Create patterns.md and system-prompt.md (Analyzer-generated placeholders)</name>
  <files>niches/ai-commentary/patterns.md, niches/ai-commentary/system-prompt.md</files>
  <action>
    Create two Analyzer-generated placeholder files in `niches/ai-commentary/`. These files are not user-editable in normal operation — the Analyzer writes them. Their headers explain this clearly and show STATUS: Not yet generated so the state is unambiguous.

    **patterns.md** — Create with this exact content:

    ```markdown
    # Patterns: ai-commentary

    **Purpose:** Cross-script synthesis — what holds true across all high-performing scripts for this niche. Patterns that appear consistently across Tier 1 and Tier 2 scripts.
    **Maintained by:** Analyzer — auto-generated and updated after training runs (do not edit manually)
    **Read by:** Generator — read first, before individual script examples, to establish niche-wide patterns before looking at specific hooks

    ---

    **STATUS: Not yet generated.**
    Run the Analyzer workflow with at least 3 training scripts to populate this file.

    <!-- Analyzer will write structured pattern observations here covering:
    - Recurring hook techniques
    - Pacing patterns
    - Sentence structure norms
    - Storytelling approaches that work for this niche
    Do not edit manually. -->
    ```

    **system-prompt.md** — Create with this exact content:

    ```markdown
    # System Prompt: ai-commentary

    **Purpose:** Niche-specific system prompt for the Generator. Encodes the creator's voice, style, and niche conventions derived from training scripts.
    **Maintained by:** Analyzer — auto-generated on first training run with 3+ scripts; can be regenerated after more scripts are ingested (do not edit manually unless intentionally overriding)
    **Read by:** Generator — injected as system context on every generation call before reading training examples

    ---

    **STATUS: Not yet generated.**
    Run the Analyzer workflow with at least 3 training scripts to generate this file.

    <!-- Analyzer will write a complete system prompt here covering:
    - Creator voice and tone
    - Niche-specific style rules
    - What makes a script succeed in this niche
    - What to avoid
    Do not edit manually unless you are intentionally overriding the Analyzer's output. -->
    ```
  </action>
  <verify>
    <automated>grep -l "Not yet generated" niches/ai-commentary/patterns.md niches/ai-commentary/system-prompt.md | wc -l</automated>
  </verify>
  <done>`patterns.md` and `system-prompt.md` both exist. Both contain "STATUS: Not yet generated." with instructions to run the Analyzer. Both document who maintains them and who reads them.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 memory structure</name>
  <what-built>
    Complete `niches/ai-commentary/` memory structure:
    - `scripts/.gitkeep` — tracks empty directory (Plan 01)
    - `SCHEMA.md` — training script format contract (Plan 01)
    - `niches.md` — top-level niche registry at project root (Plan 01)
    - `anti-patterns.md` — user-maintained, schema documented
    - `feedback.md` — user-maintained, 5-bullet cap documented
    - `patterns.md` — Analyzer placeholder, "not yet generated"
    - `system-prompt.md` — Analyzer placeholder, "not yet generated"
  </what-built>
  <how-to-verify>
    Run these commands and confirm the output looks right:

    1. Check full directory structure:
       ```
       ls -la niches/ai-commentary/
       ls -la niches/ai-commentary/scripts/
       ```
       Expected: 5 files in `ai-commentary/` (`SCHEMA.md`, `anti-patterns.md`, `feedback.md`, `patterns.md`, `system-prompt.md`) plus `scripts/` directory. `scripts/` contains only `.gitkeep`.

    2. Check niches.md:
       ```
       cat niches.md
       ```
       Expected: Table with ai-commentary row, "Adding a New Niche" section with numbered steps.

    3. Spot-check one placeholder:
       ```
       cat niches/ai-commentary/feedback.md
       ```
       Expected: Header with purpose/maintained-by/read-by/limit, "5 bullets maximum" rule, and commented example.

    4. Confirm the schema is usable — open `niches/ai-commentary/SCHEMA.md` and verify you could write a training script using it as a guide without looking at any other file.
  </how-to-verify>
  <action>Human verification checkpoint — no automated action. User inspects the file structure created by Tasks 1-2 and Plan 01.</action>
  <verify><automated>ls niches/ai-commentary/ && ls niches/ai-commentary/scripts/</automated></verify>
  <done>User types "approved" after confirming all files exist with correct content.</done>
  <resume-signal>Type "approved" if the structure looks correct, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:

```bash
ls niches/ai-commentary/
# Expected: SCHEMA.md  anti-patterns.md  feedback.md  patterns.md  scripts/  system-prompt.md

ls niches/ai-commentary/scripts/
# Expected: .gitkeep

cat niches.md | grep "ai-commentary"
# Expected: | ai-commentary | `niches/ai-commentary/` | Active | ...

grep "5 bullets" niches/ai-commentary/feedback.md
# Expected: line mentioning 5 bullets maximum

grep "Not yet generated" niches/ai-commentary/patterns.md niches/ai-commentary/system-prompt.md
# Expected: one match per file
```

All 5 phase requirements (MEM-01 through MEM-05) are satisfied by the files created across Plan 01 and Plan 02.
</verification>

<success_criteria>
- `niches/ai-commentary/anti-patterns.md` exists with purpose header, format rules, and commented example entries
- `niches/ai-commentary/feedback.md` exists with purpose header, 5-bullet cap rule explicitly documented, and commented example entries
- `niches/ai-commentary/patterns.md` exists with "STATUS: Not yet generated" and instructions to run Analyzer
- `niches/ai-commentary/system-prompt.md` exists with "STATUS: Not yet generated" and instructions to run Analyzer
- All four files have headers explaining who maintains them and who reads them
- No user-facing file is empty or has only a title — every file is self-explanatory
- User confirms structure is navigable via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/01-memory-foundation/01-02-SUMMARY.md` using the summary template.
</output>
