---
phase: 01-memory-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - niches.md
  - niches/ai-commentary/scripts/.gitkeep
  - niches/ai-commentary/SCHEMA.md
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03

must_haves:
  truths:
    - "niches/ai-commentary/ directory exists with a scripts/ subdirectory tracked by git"
    - "niches.md exists at project root listing ai-commentary and explaining how to add new niches"
    - "A training script schema reference exists that documents the exact YAML frontmatter and body format Phase 2 must write"
  artifacts:
    - path: "niches.md"
      provides: "Top-level niche registry"
      contains: "ai-commentary"
    - path: "niches/ai-commentary/scripts/.gitkeep"
      provides: "Tracked empty scripts directory"
    - path: "niches/ai-commentary/SCHEMA.md"
      provides: "Training script file contract for Phase 2 Analyzer"
      contains: "YAML frontmatter"
  key_links:
    - from: "niches.md"
      to: "niches/ai-commentary/"
      via: "directory reference in table"
      pattern: "niches/ai-commentary"
    - from: "niches/ai-commentary/SCHEMA.md"
      to: "niches/ai-commentary/scripts/"
      via: "schema documents the file naming convention and expected location"
      pattern: "scripts/"
---

<objective>
Create the niche directory structure, the top-level niche registry, and the training script schema reference. These three artifacts are the foundation contracts that Phase 2 (Analyzer) depends on to know where and how to write training script files.

Purpose: Establish the file system contracts for niche-isolated memory storage so downstream phases have a stable target to write to and read from.
Output: `niches/ai-commentary/` directory tree, `niches.md` registry, `niches/ai-commentary/SCHEMA.md` schema reference.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-memory-foundation/01-CONTEXT.md
@.planning/phases/01-memory-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create niche directory tree with git-tracked scripts directory</name>
  <files>niches/ai-commentary/scripts/.gitkeep</files>
  <action>
    Create the following directory structure at the project root:

    ```
    niches/
    └── ai-commentary/
        └── scripts/
            └── .gitkeep
    ```

    The `.gitkeep` file should be completely empty (0 bytes). Its only purpose is to ensure `niches/ai-commentary/scripts/` is tracked by git so the directory contract is visible to version control. Do NOT add any content to `.gitkeep`.

    Do NOT create `anti-patterns.md`, `feedback.md`, `patterns.md`, or `system-prompt.md` in this task — those are Plan 02.
  </action>
  <verify>
    <automated>ls niches/ai-commentary/scripts/.gitkeep</automated>
  </verify>
  <done>`niches/ai-commentary/scripts/.gitkeep` exists (empty file). `ls niches/ai-commentary/` shows `scripts/` subdirectory.</done>
</task>

<task type="auto">
  <name>Task 2: Create niches.md top-level niche registry</name>
  <files>niches.md</files>
  <action>
    Create `niches.md` at the project root (not inside `niches/` — at the top level beside it).

    Use a table format (Claude's discretion from CONTEXT.md — table preferred over list for multi-niche clarity). The file must:
    - Explain its purpose (lists all active niches)
    - Include the ai-commentary entry with directory path, status Active, and a brief description
    - Include a clear "Adding a New Niche" section with numbered steps

    Use this exact content:

    ```markdown
    # Niche Registry

    **Purpose:** Lists all active niches. To add a new niche, add a row to the table and create the corresponding directory under `niches/`.

    | Niche | Directory | Status | Description |
    |-------|-----------|--------|-------------|
    | ai-commentary | `niches/ai-commentary/` | Active | AI news commentary for YouTube Shorts |

    ## Adding a New Niche

    1. Add a row to the table above
    2. Create the directory: `niches/{niche-slug}/`
    3. Create subdirectory: `niches/{niche-slug}/scripts/`
    4. Copy `anti-patterns.md` and `feedback.md` from an existing niche and update the headers
    5. Create placeholder `patterns.md` and `system-prompt.md` (see existing niche for format)
    6. Add a `SCHEMA.md` from an existing niche — script format is the same across niches
    7. Run the Analyzer to begin ingesting training scripts

    ## File Naming Convention for Training Scripts

    Files in `scripts/` follow this pattern: `{NNN}-{hook-type-slug}.md`

    - `NNN` is a zero-padded sequential number (001, 002, 003...)
    - `hook-type-slug` is a kebab-case description of the defining hook technique

    Examples: `001-shocking-stat.md`, `002-question-hook.md`, `003-viral-claim.md`

    The number ensures predictable sort order. The slug describes the hook at a glance.
    ```
  </action>
  <verify>
    <automated>cat niches.md</automated>
  </verify>
  <done>`niches.md` exists at project root, contains the ai-commentary table row, and includes the "Adding a New Niche" section with numbered steps.</done>
</task>

<task type="auto">
  <name>Task 3: Create training script schema reference</name>
  <files>niches/ai-commentary/SCHEMA.md</files>
  <action>
    Create `niches/ai-commentary/SCHEMA.md`. This file is the contract between Phase 1 and Phase 2 — it documents the exact format the Analyzer must write and the Generator will read. It is a reference document, not a placeholder.

    The file must document:
    1. YAML frontmatter: all 5 fields with correct types and valid values
    2. Body structure: `## Transcript` with [HOOK]/[BUILD]/[PAYOFF] labels, then `## Analysis` with all 6 fields
    3. Field-level rules (integer types, what each field means, analysis length cap)
    4. A complete filled example

    Use this exact content:

    ```markdown
    # Training Script Schema: ai-commentary

    **Purpose:** Defines the exact format for training script files in `scripts/`. The Analyzer (Phase 2) writes files in this format. The Generator (Phase 3) reads them.
    **Location:** `niches/ai-commentary/scripts/{NNN}-{hook-type-slug}.md`

    ---

    ## YAML Frontmatter

    All five fields are required. Use native YAML types — not strings that contain numbers.

    | Field | Type | Valid Values | Example |
    |-------|------|--------------|---------|
    | `title` | string | Any — the script's topic or headline | `"AI Just Passed the Bar Exam"` |
    | `views` | integer | Whole number, no commas or abbreviations | `2400000` |
    | `quality_tier` | integer | `1` (>1M views), `2` (100K–1M), `3` (<100K) | `1` |
    | `hook_type` | string | kebab-case slug describing the hook technique | `"shocking-stat"` |
    | `date_analyzed` | string | ISO date YYYY-MM-DD | `"2026-03-31"` |

    **Do NOT add extra fields.** The `source` field was explicitly removed — all scripts are YouTube Shorts.

    ---

    ## Body Structure

    ### ## Transcript

    Label each section. No blank lines between the label and the first line of content.

    ```
    [HOOK]
    {hook text — typically 1–2 sentences, ~2–3 seconds when spoken}

    [BUILD]
    {build text — typically 3–6 sentences, ~15–20 seconds when spoken}

    [PAYOFF]
    {payoff text — typically 1–2 sentences, ~3–5 seconds when spoken}
    ```

    ### ## Analysis

    Six fields. Each capped at 2–3 sentences for token efficiency. Use bold field names.

    | Field | What it captures |
    |-------|-----------------|
    | `writing_style` | Tone, register, energy level |
    | `hook_patterns` | Specific technique used in the hook |
    | `pacing` | How energy and timing flow through the script |
    | `sentence_structure` | Length, rhythm, question vs statement ratio |
    | `storytelling_technique` | Setup/tension/reveal, journey, contrast, etc. |
    | `why_it_works` | Overall summary of what makes this script perform |

    ---

    ## Complete Example

    ```markdown
    ---
    title: "AI Just Passed the Bar Exam"
    views: 2400000
    quality_tier: 1
    hook_type: shocking-stat
    date_analyzed: 2026-03-31
    ---

    ## Transcript

    [HOOK]
    This AI just passed the bar exam. Who's getting fired first?

    [BUILD]
    GPT-4 scored in the 90th percentile — better than most human lawyers who took it last year.
    But here's what nobody's talking about: it didn't just memorize case law.
    It reasoned through hypotheticals. The kind law school tests you on.

    [PAYOFF]
    Your lawyer charges $400 an hour to do what a $20 API call just did better.

    ## Analysis

    **writing_style:** Clipped, declarative sentences. No hedging. Tone is confrontational without being hostile — forces the viewer to pick a side.

    **hook_patterns:** Shocking stat delivered as a bald statement, immediately followed by a direct-address implication question. No wind-up, no context-setting.

    **pacing:** Opens at maximum tension, sustains through the BUILD with escalating specificity ("90th percentile," "hypotheticals"), lands the PAYOFF as a dollar-amount gut punch.

    **sentence_structure:** 70% short declaratives (under 10 words), 30% medium compound. No questions except in the hook. No filler words.

    **storytelling_technique:** Contrast — everyone knows AI is smart, but scoring above most lawyers reframes the stakes. The payoff flips from abstract ("AI is impressive") to personal ("your money").

    **why_it_works:** The hook assumes the viewer's conclusion before they reach it. The build makes the assumption feel inevitable. The payoff makes it personal.
    ```

    ---

    ## Common Mistakes to Avoid

    - `views: "2.4M"` — wrong. Use `views: 2400000` (integer, no quotes)
    - `quality_tier: "Tier 1"` — wrong. Use `quality_tier: 1` (integer)
    - Adding a `source:` field — explicitly removed. Do not add it.
    - Analysis fields longer than 2–3 sentences — hurts token efficiency at generation time
    ```
  </action>
  <verify>
    <automated>cat niches/ai-commentary/SCHEMA.md | grep "quality_tier"</automated>
  </verify>
  <done>`niches/ai-commentary/SCHEMA.md` exists and contains all 5 frontmatter fields, the [HOOK]/[BUILD]/[PAYOFF] transcript structure, all 6 analysis fields, and a complete filled example.</done>
</task>

</tasks>

<verification>
Run after all tasks complete:

```bash
ls -la niches/ai-commentary/
ls -la niches/ai-commentary/scripts/
cat niches.md
grep -c "quality_tier" niches/ai-commentary/SCHEMA.md
```

Expected: `niches/ai-commentary/` contains `scripts/` and `SCHEMA.md`. `scripts/` contains `.gitkeep`. `niches.md` shows ai-commentary table row. SCHEMA.md contains `quality_tier`.
</verification>

<success_criteria>
- `niches/ai-commentary/scripts/.gitkeep` exists (empty file, directory tracked by git)
- `niches.md` at project root lists ai-commentary in a table with directory, status, and description
- `niches.md` includes a numbered "Adding a New Niche" section and file naming convention
- `niches/ai-commentary/SCHEMA.md` documents all 5 YAML frontmatter fields with correct types
- `niches/ai-commentary/SCHEMA.md` documents all 6 Analysis fields with 2-3 sentence cap rule
- `niches/ai-commentary/SCHEMA.md` includes a complete filled script example
- No extra YAML fields added (no `source:` field)
</success_criteria>

<output>
After completion, create `.planning/phases/01-memory-foundation/01-01-SUMMARY.md` using the summary template.
</output>
